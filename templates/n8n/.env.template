# n8n Configuration
# Required: Domain for webhook URLs and SSL (leave empty for IP-only access)
DOMAIN=your-domain.com

# Required: n8n admin credentials
N8N_USER=admin
N8N_PASSWORD=changeme

# Required: PostgreSQL password (auto-generated recommended)
POSTGRES_PASSWORD=changeme

# Optional: Port override (default: 5678)
N8N_PORT=5678

# Optional: Protocol (https if domain set, http otherwise)
N8N_PROTOCOL=https
